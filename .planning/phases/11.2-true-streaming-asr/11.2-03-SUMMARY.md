---
phase: 11.2-true-streaming-asr
plan: 03
subsystem: queue-and-parakeet
tags: [parakeet, fluidaudio, queue, backpressure]
provides:
  - "Priority fragment lanes in TranscriptionQueue"
  - "Parakeet streaming sessions with per-source isolation"
  - "EOU callback bridge back into MeetingRecorder"
affects: [TranscriptionQueue, ParakeetVoiceToTextModel]
key-files:
  created: []
  modified: [Sources/TranscriptionQueue.swift, Sources/ParakeetVoiceToTextModel.swift]
requirements-completed: [LIVE-05, LIVE-06]
completed: 2026-04-19
---

# Phase 11.2 Plan 03 Summary

Upgraded the shared transcription queue for fragment-level priority scheduling and added real Parakeet streaming sessions on top of FluidAudio's `StreamingEouAsrManager`.

## Accomplishments
- Added microphone and system fragment lanes, microphone-first scheduling, and 2.0s system backpressure eviction.
- Exposed `enqueueFragment` and `totalQueuedDuration(for:)` for recorder wiring and tests.
- Added per-source Parakeet streaming sessions, partial-token callbacks, and EOU callbacks dispatched back to the main actor.

## Verification
- `swift test --filter StreamingTests`
- `swift test --parallel --verbose`

## Self-Check: PASSED
