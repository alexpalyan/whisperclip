---
phase: 02-viewmodel-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/HotkeySettingsViewModel.swift
  - Sources/HotkeySettingsView.swift
autonomous: true
requirements: [VM-01, VM-04]
must_haves:
  truths:
    - "Hotkey modifier/key selection persists through SettingsStore"
    - "updateHotkey() and updateMeetingHotkey() call HotkeyManager methods"
    - "HotkeySettingsView contains zero private business-logic functions"
    - "HotkeySettingsViewModel does not import SwiftUI"
  artifacts:
    - path: "Sources/HotkeySettingsViewModel.swift"
      provides: "Hotkey business logic as ObservableObject"
      contains: "class HotkeySettingsViewModel: ObservableObject"
    - path: "Sources/HotkeySettingsView.swift"
      provides: "Declarative hotkey tab using VM bindings"
      contains: "@StateObject var vm = HotkeySettingsViewModel()"
  key_links:
    - from: "Sources/HotkeySettingsView.swift"
      to: "Sources/HotkeySettingsViewModel.swift"
      via: "@StateObject var vm"
      pattern: "\\$vm\\."
    - from: "Sources/HotkeySettingsViewModel.swift"
      to: "Sources/SettingsStore.swift"
      via: "constructor injection"
      pattern: "store: SettingsStore = \\.shared"
    - from: "Sources/HotkeySettingsViewModel.swift"
      to: "Sources/HotkeyManager.swift"
      via: "constructor injection"
      pattern: "hotkeyManager: HotkeyManager = \\.shared"
---

<objective>
Extract all hotkey business logic from HotkeySettingsView into HotkeySettingsViewModel.

Purpose: Separate hotkey state management and side effects (HotkeyManager calls) from UI rendering, enabling testability with injectable SettingsStore and HotkeyManager.
Output: New HotkeySettingsViewModel.swift, refactored HotkeySettingsView.swift with only bindings.
</objective>

<execution_context>
@/Users/oleksandrpalan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oleksandrpalan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-viewmodel-extraction/02-CONTEXT.md
@.planning/phases/02-viewmodel-extraction/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Sources/SettingsStore.swift:
```swift
class SettingsStore: ObservableObject {
    static let shared = SettingsStore()
    @Published var hotkeyEnabled: Bool = true
    @Published var hotkeyModifier: NSEvent.ModifierFlags = DefaultSettings.hotkeyModifier
    @Published var hotkeyKey: UInt16 = DefaultSettings.hotkeyKey
    @Published var meetingHotkeyEnabled: Bool = DefaultSettings.meetingHotkeyEnabled
    @Published var meetingHotkeyModifier: NSEvent.ModifierFlags = DefaultSettings.meetingHotkeyModifier
    @Published var meetingHotkeyKey: UInt16 = DefaultSettings.meetingHotkeyKey
    @Published var holdToTalk: Bool = DefaultSettings.holdToTalk
}
```

From Sources/HotkeyManager.swift:
```swift
class HotkeyManager: ObservableObject {
    static let shared = HotkeyManager()
    static let meetingShared = HotkeyManager()
    func updateSystemHotkey(hotkeyEnabled: Bool, modifier: NSEvent.ModifierFlags, keyCode: UInt16)
}
```

From Sources/SettingsViewData.swift:
```swift
// Static data arrays used by hotkey views:
static let modifierOptions: [(UInt, String)]
static let keyOptions: [(UInt16, String)]
static let meetingKeyOptions: [(UInt16, String)]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HotkeySettingsViewModel with all hotkey logic</name>
  <files>Sources/HotkeySettingsViewModel.swift</files>
  <read_first>
    Sources/HotkeySettingsView.swift
    Sources/SettingsStore.swift
    Sources/HotkeyManager.swift
    Sources/SettingsViewData.swift
  </read_first>
  <action>
Create `Sources/HotkeySettingsViewModel.swift` with:

**Imports:** `import Foundation` and `import Cocoa` (for NSEvent.ModifierFlags). Do NOT import SwiftUI.

**Class definition:**
```swift
@MainActor
class HotkeySettingsViewModel: ObservableObject {
    private let store: SettingsStore
    private let hotkeyManager: HotkeyManager
    private let meetingHotkeyManager: HotkeyManager

    init(store: SettingsStore = .shared,
         hotkeyManager: HotkeyManager = .shared,
         meetingHotkeyManager: HotkeyManager = .meetingShared)
}
```

**@Published properties** (match current @State names from HotkeySettingsView):
- `selectedModifierRawValue: UInt` - init from `store.hotkeyModifier.rawValue`
- `hotkeyKeyString: String` - init from `keyCodeToString(store.hotkeyKey)`
- `selectedKeyCode: UInt16` - init from `store.hotkeyKey`
- `meetingModifierRawValue: UInt` - init from `store.meetingHotkeyModifier.rawValue`
- `meetingKeyString: String` - init from `meetingKeyCodeToString(store.meetingHotkeyKey)`
- `meetingKeyCode: UInt16` - init from `store.meetingHotkeyKey`

**Methods to move from HotkeySettingsView** (copy logic exactly, replace `settings.` with `store.`):
- `func updateHotkey()` - calls `hotkeyManager.updateSystemHotkey(hotkeyEnabled: store.hotkeyEnabled, modifier: store.hotkeyModifier, keyCode: store.hotkeyKey)`
- `func updateMeetingHotkey()` - calls `meetingHotkeyManager.updateSystemHotkey(hotkeyEnabled: store.meetingHotkeyEnabled, modifier: store.meetingHotkeyModifier, keyCode: store.meetingHotkeyKey)`
- `func getModifierString() -> String` - looks up `store.hotkeyModifier.rawValue` in `SettingsViewData.modifierOptions`, returns display string
- `func getMeetingModifierString() -> String` - looks up `store.meetingHotkeyModifier.rawValue` in `SettingsViewData.modifierOptions`, returns display string
- `func keyCodeToString(_ keyCode: UInt16) -> String` - looks up in `SettingsViewData.keyOptions`
- `func meetingKeyCodeToString(_ keyCode: UInt16) -> String` - looks up in `SettingsViewData.meetingKeyOptions`
- `func loadHotkeySettings()` - syncs all 6 @Published properties from `store`

**onChange handlers as methods** (called by View's onChange):
- `func onModifierChanged(_ newValue: UInt)` - sets `store.hotkeyModifier = NSEvent.ModifierFlags(rawValue: newValue)`, calls `updateHotkey()`
- `func onKeyCodeChanged(_ newValue: UInt16)` - sets `store.hotkeyKey = newValue`, updates `hotkeyKeyString`, calls `updateHotkey()`
- `func onHotkeyEnabledChanged(_ newValue: Bool)` - sets `store.hotkeyEnabled = newValue`, calls `updateHotkey()`
- `func onMeetingModifierChanged(_ newValue: UInt)` - sets `store.meetingHotkeyModifier = NSEvent.ModifierFlags(rawValue: newValue)`, calls `updateMeetingHotkey()`
- `func onMeetingKeyCodeChanged(_ newValue: UInt16)` - sets `store.meetingHotkeyKey = newValue`, updates `meetingKeyString`, calls `updateMeetingHotkey()`
- `func onMeetingHotkeyEnabledChanged(_ newValue: Bool)` - sets `store.meetingHotkeyEnabled = newValue`, calls `updateMeetingHotkey()`
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sources/HotkeySettingsViewModel.swift contains `class HotkeySettingsViewModel: ObservableObject`
    - Sources/HotkeySettingsViewModel.swift contains `import Foundation`
    - Sources/HotkeySettingsViewModel.swift contains `import Cocoa`
    - Sources/HotkeySettingsViewModel.swift does NOT contain `import SwiftUI`
    - Sources/HotkeySettingsViewModel.swift contains `@MainActor`
    - Sources/HotkeySettingsViewModel.swift contains `store: SettingsStore = .shared`
    - Sources/HotkeySettingsViewModel.swift contains `hotkeyManager: HotkeyManager = .shared`
    - Sources/HotkeySettingsViewModel.swift contains `meetingHotkeyManager: HotkeyManager = .meetingShared`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var selectedModifierRawValue: UInt`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var hotkeyKeyString: String`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var selectedKeyCode: UInt16`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var meetingModifierRawValue: UInt`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var meetingKeyString: String`
    - Sources/HotkeySettingsViewModel.swift contains `@Published var meetingKeyCode: UInt16`
    - Sources/HotkeySettingsViewModel.swift contains `func updateHotkey()`
    - Sources/HotkeySettingsViewModel.swift contains `func updateMeetingHotkey()`
    - Sources/HotkeySettingsViewModel.swift contains `func getModifierString() -> String`
    - Sources/HotkeySettingsViewModel.swift contains `func keyCodeToString(_ keyCode: UInt16) -> String`
    - Sources/HotkeySettingsViewModel.swift contains `func loadHotkeySettings()`
    - `swift build` exits 0
  </acceptance_criteria>
  <done>HotkeySettingsViewModel.swift exists with all 6 @Published properties, all hotkey methods, injectable init with SettingsStore + HotkeyManager, @MainActor, no SwiftUI import, project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HotkeySettingsView to HotkeySettingsViewModel</name>
  <files>Sources/HotkeySettingsView.swift</files>
  <read_first>
    Sources/HotkeySettingsView.swift
    Sources/HotkeySettingsViewModel.swift
  </read_first>
  <action>
Refactor `Sources/HotkeySettingsView.swift` to use the new ViewModel:

**Add ViewModel ownership:**
```swift
@StateObject var vm = HotkeySettingsViewModel()
```

**Remove these 6 @State properties** (now in VM):
- `selectedModifierRawValue`
- `hotkeyKeyString`
- `selectedKeyCode`
- `meetingModifierRawValue`
- `meetingKeyString`
- `meetingKeyCode`

**Remove these private functions** (now in VM):
- `loadHotkeySettings()`
- `updateHotkey()`
- `updateMeetingHotkey()`
- `getModifierString()`
- `getMeetingModifierString()`
- `keyCodeToString(_:)`
- `meetingKeyCodeToString(_:)`

**Update all references in body:**
- `$selectedModifierRawValue` -> `$vm.selectedModifierRawValue`
- `$selectedKeyCode` -> `$vm.selectedKeyCode`
- `$meetingModifierRawValue` -> `$vm.meetingModifierRawValue`
- `$meetingKeyCode` -> `$vm.meetingKeyCode`
- `hotkeyKeyString` -> `vm.hotkeyKeyString`
- `meetingKeyString` -> `vm.meetingKeyString`
- `getModifierString()` -> `vm.getModifierString()`
- `getMeetingModifierString()` -> `vm.getMeetingModifierString()`

**Update onChange handlers:**
- Modifier onChange: call `vm.onModifierChanged(newValue)` instead of inline logic
- Key onChange: call `vm.onKeyCodeChanged(newValue)` instead of inline logic
- Meeting modifier onChange: call `vm.onMeetingModifierChanged(newValue)` instead of inline logic
- Meeting key onChange: call `vm.onMeetingKeyCodeChanged(newValue)` instead of inline logic

**Update Toggle Bindings:**
- "Enable Global Hotkey" toggle set: call `vm.onHotkeyEnabledChanged(newValue)` instead of `updateHotkey()`
- "Enable Meeting Hotkey" toggle set: call `vm.onMeetingHotkeyEnabledChanged(newValue)` instead of `updateMeetingHotkey()`

**Update onAppear:**
- Replace `loadHotkeySettings()` with `vm.loadHotkeySettings()`

**Update external onChange observers:**
- `.onChange(of: settings.hotkeyModifier)` -> update `vm.selectedModifierRawValue = newValue.rawValue`
- `.onChange(of: settings.hotkeyKey)` -> update `vm.selectedKeyCode = newValue` and `vm.hotkeyKeyString = vm.keyCodeToString(newValue)`

**Keep unchanged:**
- `@ObservedObject var settings: SettingsStore` - Views still read `settings.hotkeyEnabled`, `settings.holdToTalk`, etc. for display
- `settings.holdToTalk` binding in "Hold to talk" Toggle
- All UI layout code
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sources/HotkeySettingsView.swift contains `@StateObject var vm = HotkeySettingsViewModel()`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var selectedModifierRawValue`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var hotkeyKeyString`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var selectedKeyCode`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var meetingModifierRawValue`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var meetingKeyString`
    - Sources/HotkeySettingsView.swift does NOT contain `@State private var meetingKeyCode`
    - Sources/HotkeySettingsView.swift does NOT contain `private func updateHotkey()`
    - Sources/HotkeySettingsView.swift does NOT contain `private func loadHotkeySettings()`
    - Sources/HotkeySettingsView.swift does NOT contain `private func getModifierString()`
    - Sources/HotkeySettingsView.swift does NOT contain `private func keyCodeToString`
    - Sources/HotkeySettingsView.swift contains `$vm.selectedModifierRawValue`
    - Sources/HotkeySettingsView.swift contains `vm.getModifierString()`
    - Sources/HotkeySettingsView.swift contains `vm.loadHotkeySettings()`
    - `swift build` exits 0
  </acceptance_criteria>
  <done>HotkeySettingsView has zero private business logic functions. All state bindings use `vm.` prefix. Only @State remaining would be pure UI modals (none in this view). Project compiles.</done>
</task>

</tasks>

<verification>
- `swift build` passes with no errors
- `HotkeySettingsViewModel.swift` exists with ObservableObject, @MainActor, injectable init
- `HotkeySettingsView.swift` has no private business logic functions
- All hotkey state flows through ViewModel
</verification>

<success_criteria>
1. HotkeySettingsViewModel encapsulates all 6 state properties and all hotkey methods per VM-01
2. HotkeySettingsView is declarative — only bindings and rendering per VM-04
3. SettingsStore and HotkeyManager are injected via init for testability
4. No `import SwiftUI` in ViewModel
5. `swift build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-viewmodel-extraction/02-01-SUMMARY.md`
</output>
