---
phase: 02-viewmodel-extraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/PromptManagementViewModel.swift
  - Sources/PromptsSettingsView.swift
autonomous: true
requirements: [VM-03, VM-04]
must_haves:
  truths:
    - "Prompt create/edit/delete/cancel flows through PromptManagementViewModel"
    - "PromptsSettingsView contains zero private business-logic functions"
    - "PromptManagementViewModel does not import SwiftUI"
    - "New prompt dialog state is managed by ViewModel"
  artifacts:
    - path: "Sources/PromptManagementViewModel.swift"
      provides: "Prompt CRUD state as ObservableObject"
      contains: "class PromptManagementViewModel: ObservableObject"
    - path: "Sources/PromptsSettingsView.swift"
      provides: "Declarative prompts tab using VM bindings"
      contains: "@StateObject var vm = PromptManagementViewModel()"
  key_links:
    - from: "Sources/PromptsSettingsView.swift"
      to: "Sources/PromptManagementViewModel.swift"
      via: "@StateObject var vm"
      pattern: "\\$vm\\."
    - from: "Sources/PromptManagementViewModel.swift"
      to: "Sources/SettingsStore.swift"
      via: "constructor injection"
      pattern: "store: SettingsStore = \\.shared"
---

<objective>
Extract prompt CRUD state and functions from PromptsSettingsView into PromptManagementViewModel.

Purpose: Separate prompt management logic from UI rendering, enabling testability with injectable SettingsStore.
Output: New PromptManagementViewModel.swift, refactored PromptsSettingsView.swift with only bindings.
</objective>

<execution_context>
@/Users/oleksandrpalan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oleksandrpalan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-viewmodel-extraction/02-CONTEXT.md
@.planning/phases/02-viewmodel-extraction/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Sources/SettingsStore.swift:
```swift
struct Prompt: Codable, Identifiable {
    let id: String
    var label: String
    var content: String
    init(label: String, content: String)
}

class SettingsStore: ObservableObject {
    static let shared = SettingsStore()
    @Published var prompts: [Prompt] = []
    @Published var selectedPromptId: String? = nil
    func createPrompt(label: String, content: String = "") -> Prompt
    func updatePrompt(id: String, label: String? = nil, content: String? = nil)
    func deletePrompt(id: String)
    func selectPrompt(id: String)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PromptManagementViewModel with prompt CRUD logic</name>
  <files>Sources/PromptManagementViewModel.swift</files>
  <read_first>
    Sources/PromptsSettingsView.swift
    Sources/SettingsStore.swift
  </read_first>
  <action>
Create `Sources/PromptManagementViewModel.swift` with:

**Imports:** `import Foundation`. Do NOT import SwiftUI.

**Class definition:**
```swift
@MainActor
class PromptManagementViewModel: ObservableObject {
    private let store: SettingsStore

    init(store: SettingsStore = .shared)
}
```

**@Published properties** (match current @State names from PromptsSettingsView):
- `showingNewPromptDialog: Bool = false`
- `newPromptLabel: String = ""`
- `newPromptContent: String = ""`
- `editingPromptId: String? = nil`
- `editingPromptLabel: String = ""`
- `editingPromptContent: String = ""`

**Methods to move from PromptsSettingsView** (copy logic exactly, replace `settings.` with `store.`):

- `func createNewPrompt()` - guards `newPromptLabel` not empty after trimming, calls `store.createPrompt(label: newPromptLabel, content: newPromptContent)`, resets `newPromptLabel = ""`, `newPromptContent = ""`, `showingNewPromptDialog = false`.

- `func cancelNewPrompt()` - resets `newPromptLabel = ""`, `newPromptContent = ""`, `showingNewPromptDialog = false`.

- `func startEditing(_ prompt: Prompt)` - sets `editingPromptId = prompt.id`, `editingPromptLabel = prompt.label`, `editingPromptContent = prompt.content`.

- `func savePromptEdits(_ promptId: String)` - calls `store.updatePrompt(id: promptId, label: editingPromptLabel, content: editingPromptContent)`, then calls `cancelEditing()`.

- `func cancelEditing()` - resets `editingPromptId = nil`, `editingPromptLabel = ""`, `editingPromptContent = ""`.
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sources/PromptManagementViewModel.swift contains `class PromptManagementViewModel: ObservableObject`
    - Sources/PromptManagementViewModel.swift contains `import Foundation`
    - Sources/PromptManagementViewModel.swift does NOT contain `import SwiftUI`
    - Sources/PromptManagementViewModel.swift contains `@MainActor`
    - Sources/PromptManagementViewModel.swift contains `store: SettingsStore = .shared`
    - Sources/PromptManagementViewModel.swift contains `@Published var showingNewPromptDialog`
    - Sources/PromptManagementViewModel.swift contains `@Published var newPromptLabel`
    - Sources/PromptManagementViewModel.swift contains `@Published var newPromptContent`
    - Sources/PromptManagementViewModel.swift contains `@Published var editingPromptId`
    - Sources/PromptManagementViewModel.swift contains `@Published var editingPromptLabel`
    - Sources/PromptManagementViewModel.swift contains `@Published var editingPromptContent`
    - Sources/PromptManagementViewModel.swift contains `func createNewPrompt()`
    - Sources/PromptManagementViewModel.swift contains `func startEditing(_ prompt: Prompt)`
    - Sources/PromptManagementViewModel.swift contains `func savePromptEdits(_ promptId: String)`
    - Sources/PromptManagementViewModel.swift contains `func cancelEditing()`
    - Sources/PromptManagementViewModel.swift contains `func cancelNewPrompt()`
    - Sources/PromptManagementViewModel.swift contains `store.createPrompt`
    - Sources/PromptManagementViewModel.swift contains `store.updatePrompt`
    - `swift build` exits 0
  </acceptance_criteria>
  <done>PromptManagementViewModel.swift exists with all 6 @Published properties, all prompt CRUD methods, injectable init with SettingsStore, @MainActor, no SwiftUI import, project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PromptsSettingsView to PromptManagementViewModel</name>
  <files>Sources/PromptsSettingsView.swift</files>
  <read_first>
    Sources/PromptsSettingsView.swift
    Sources/PromptManagementViewModel.swift
  </read_first>
  <action>
Refactor `Sources/PromptsSettingsView.swift` to use the new ViewModel:

**Add ViewModel ownership:**
```swift
@StateObject var vm = PromptManagementViewModel()
```

**Remove these 6 @State properties** (now in VM):
- `showingNewPromptDialog`
- `newPromptLabel`
- `newPromptContent`
- `editingPromptId`
- `editingPromptLabel`
- `editingPromptContent`

**Remove these private functions** (now in VM):
- `createNewPrompt()`
- `cancelNewPrompt()`
- `startEditing(_:)`
- `savePromptEdits(_:)`
- `cancelEditing()`

**Update all references in body:**

In the "New Prompt" button:
- `showingNewPromptDialog = true` -> `vm.showingNewPromptDialog = true`

In the .sheet binding:
- `$showingNewPromptDialog` -> `$vm.showingNewPromptDialog`

In the NewPromptDialog:
- `$newPromptLabel` -> `$vm.newPromptLabel`
- `$newPromptContent` -> `$vm.newPromptContent`
- `onCreate: { createNewPrompt() }` -> `onCreate: { vm.createNewPrompt() }`
- `onCancel: { cancelNewPrompt() }` -> `onCancel: { vm.cancelNewPrompt() }`

In the PromptRowView ForEach:
- `isEditing: editingPromptId == prompt.id` -> `isEditing: vm.editingPromptId == prompt.id`
- `editingLabel: $editingPromptLabel` -> `editingLabel: $vm.editingPromptLabel`
- `editingContent: $editingPromptContent` -> `editingContent: $vm.editingPromptContent`
- `onEdit: { startEditing(prompt) }` -> `onEdit: { vm.startEditing(prompt) }`
- `onSave: { savePromptEdits(prompt.id) }` -> `onSave: { vm.savePromptEdits(prompt.id) }`
- `onCancel: { cancelEditing() }` -> `onCancel: { vm.cancelEditing() }`

**Keep unchanged:**
- `@ObservedObject var settings: SettingsStore` - View still reads `settings.prompts`, `settings.selectedPromptId` for display
- `onSelect: { settings.selectPrompt(id: prompt.id) }` - direct store call for selection
- `onDelete: { settings.deletePrompt(id: prompt.id) }` - direct store call for deletion
- `PromptRowView` and `NewPromptDialog` struct definitions - unchanged
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sources/PromptsSettingsView.swift contains `@StateObject var vm = PromptManagementViewModel()`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var showingNewPromptDialog`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var newPromptLabel`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var newPromptContent`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var editingPromptId`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var editingPromptLabel`
    - Sources/PromptsSettingsView.swift does NOT contain `@State private var editingPromptContent`
    - Sources/PromptsSettingsView.swift does NOT contain `private func createNewPrompt`
    - Sources/PromptsSettingsView.swift does NOT contain `private func startEditing`
    - Sources/PromptsSettingsView.swift does NOT contain `private func savePromptEdits`
    - Sources/PromptsSettingsView.swift does NOT contain `private func cancelEditing`
    - Sources/PromptsSettingsView.swift contains `$vm.showingNewPromptDialog`
    - Sources/PromptsSettingsView.swift contains `vm.createNewPrompt()`
    - Sources/PromptsSettingsView.swift contains `vm.startEditing(prompt)`
    - Sources/PromptsSettingsView.swift contains `vm.savePromptEdits(prompt.id)`
    - `swift build` exits 0
  </acceptance_criteria>
  <done>PromptsSettingsView has zero private business logic functions. All prompt CRUD state bindings use `vm.` prefix. No @State properties remain. Project compiles.</done>
</task>

</tasks>

<verification>
- `swift build` passes with no errors
- `PromptManagementViewModel.swift` exists with ObservableObject, @MainActor, injectable init
- `PromptsSettingsView.swift` has no private business logic functions
- All prompt CRUD state flows through ViewModel
</verification>

<success_criteria>
1. PromptManagementViewModel encapsulates all prompt dialog/editing state and CRUD methods per VM-03
2. PromptsSettingsView is declarative — only bindings and rendering per VM-04
3. SettingsStore is injected via init for testability
4. No `import SwiftUI` in ViewModel
5. `swift build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-viewmodel-extraction/02-03-SUMMARY.md`
</output>
