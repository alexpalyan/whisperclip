---
phase: 09-waveform-color-per-speaker
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Tests/SharedViewsTests.swift
  - Tests/WaveformHistoryTests.swift
  - Tests/SpeakerResolutionTests.swift
autonomous: true
requirements: [RT-01, RT-02, RT-03]

must_haves:
  truths:
    - "Test stubs exist for all three test files before implementation tasks run"
    - "swift test compiles and runs the stubs (they may be empty or trivially passing)"
  artifacts:
    - path: "Tests/SharedViewsTests.swift"
      provides: "XCTest stub for speakerPaletteColor hex palette verification"
      contains: "class SharedViewsTests"
    - path: "Tests/WaveformHistoryTests.swift"
      provides: "XCTest stub for waveform history append/reset logic"
      contains: "class WaveformHistoryTests"
    - path: "Tests/SpeakerResolutionTests.swift"
      provides: "XCTest stub for Speaker(displayName:) resolution"
      contains: "class SpeakerResolutionTests"
  key_links: []
---

<objective>
Create Wave 0 test stubs for Phase 9. These three XCTest files establish the test targets that implementation plans (09-01, 09-02) will populate with real assertions.

Purpose: Nyquist compliance requires test files to exist before implementation tasks reference them in verify commands. Without Wave 0, `swift test --filter SharedViewsTests` would fail with "no matching test" rather than a meaningful red/green signal.

Output: Three test stub files in Tests/ directory, each with a single placeholder test.
</objective>

<execution_context>
@/Users/oleksandrpalan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oleksandrpalan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-waveform-color-per-speaker/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three XCTest stub files for Phase 9</name>
  <files>Tests/SharedViewsTests.swift, Tests/WaveformHistoryTests.swift, Tests/SpeakerResolutionTests.swift</files>
  <read_first>
    - Tests/SpeakerBufferManagerTests.swift
  </read_first>
  <action>
Create three test stub files following the existing XCTest pattern in the project (see SpeakerBufferManagerTests.swift for import style).

1. **Tests/SharedViewsTests.swift** — stub for speakerPaletteColor palette verification:
   ```swift
   import XCTest
   @testable import WhisperClip

   final class SharedViewsTests: XCTestCase {

       // MARK: - RT-02: Hex palette colors

       func testSpeakerPaletteColorReturnsHexBasedColor() {
           // Will be expanded in 09-01 to verify each palette index returns the correct hex color
           // Placeholder: verify function is callable
           let color = speakerPaletteColor(.me)
           XCTAssertNotNil(color)
       }
   }
   ```

2. **Tests/WaveformHistoryTests.swift** — stub for waveform history logic:
   ```swift
   import XCTest
   @testable import WhisperClip

   final class WaveformHistoryTests: XCTestCase {

       // MARK: - RT-01: Waveform history append and reset

       func testPlaceholder() {
           // Will be expanded in 09-02 to verify:
           // - Parallel arrays (levels + speakerLabels) append correctly
           // - Arrays reset on new recording
           // - Canvas renders correct bar count
           XCTAssertTrue(true, "Stub — real tests added in 09-02")
       }
   }
   ```

3. **Tests/SpeakerResolutionTests.swift** — stub for Speaker(displayName:) resolution:
   ```swift
   import XCTest
   @testable import WhisperClip

   final class SpeakerResolutionTests: XCTestCase {

       // MARK: - RT-03: Speaker display name resolution

       func testSpeakerDisplayNameResolution() {
           // Will be expanded in 09-01 to verify:
           // - "Me" -> .me
           // - "" -> .unknown
           // - "Speaker 1" -> .labeled("Speaker 1")
           // Placeholder: verify enum case construction
           let speaker = Speaker.me
           XCTAssertEqual(speaker.displayName, "Me")
       }
   }
   ```

Key points:
- All three files use `@testable import WhisperClip` (matching project convention)
- Each stub contains at least one non-trivial assertion so `swift test --filter` produces a green result
- Comments describe the real tests that will be added in Plans 01 and 02
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift test --filter "SharedViewsTests|WaveformHistoryTests|SpeakerResolutionTests" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Tests/SharedViewsTests.swift exists and contains `class SharedViewsTests: XCTestCase`
    - Tests/WaveformHistoryTests.swift exists and contains `class WaveformHistoryTests: XCTestCase`
    - Tests/SpeakerResolutionTests.swift exists and contains `class SpeakerResolutionTests: XCTestCase`
    - `swift test --filter SharedViewsTests` passes
    - `swift test --filter WaveformHistoryTests` passes
    - `swift test --filter SpeakerResolutionTests` passes
  </acceptance_criteria>
  <done>Three test stub files compile and pass. Wave 0 complete — implementation plans can use swift test --filter as automated verify.</done>
</task>

</tasks>

<verification>
- `swift test --filter SharedViewsTests` passes
- `swift test --filter WaveformHistoryTests` passes
- `swift test --filter SpeakerResolutionTests` passes
- All three files exist in Tests/ directory
</verification>

<success_criteria>
- Three XCTest stub files created in Tests/
- All stubs compile and pass with `swift test`
- Wave 0 prerequisites satisfied for Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/09-waveform-color-per-speaker/09-00-SUMMARY.md`
</output>
