---
phase: 01-ui-decomposition
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - Sources/PromptsSettingsView.swift
  - Sources/SettingsView.swift
autonomous: true
requirements: [UI-04, UI-06]

must_haves:
  truths:
    - "PromptsSettingsView.swift renders the Prompts tab identically to the original including PromptRowView and NewPromptDialog"
    - "SettingsView.swift is a thin TabView coordinator with no tab content, no @State business logic, no helper functions"
    - "SettingsView.swift is 80 lines or fewer"
    - "Project compiles with swift build"
  artifacts:
    - path: "Sources/PromptsSettingsView.swift"
      provides: "Prompts settings tab with PromptRowView and NewPromptDialog"
      contains: "struct PromptsSettingsView"
    - path: "Sources/SettingsView.swift"
      provides: "Thin TabView coordinator (~60 lines)"
  key_links:
    - from: "Sources/PromptsSettingsView.swift"
      to: "Sources/SettingsStore.swift"
      via: "@ObservedObject var settings: SettingsStore"
      pattern: "@ObservedObject var settings: SettingsStore"
    - from: "Sources/SettingsView.swift"
      to: "Sources/PromptsSettingsView.swift"
      via: "PromptsSettingsView(settings: settings) in TabView"
      pattern: "PromptsSettingsView\\(settings:"
    - from: "Sources/SettingsView.swift"
      to: "Sources/GeneralSettingsView.swift"
      via: "GeneralSettingsView(settings: settings)"
      pattern: "GeneralSettingsView\\(settings:"
    - from: "Sources/SettingsView.swift"
      to: "Sources/HotkeySettingsView.swift"
      via: "HotkeySettingsView(settings: settings)"
      pattern: "HotkeySettingsView\\(settings:"
    - from: "Sources/SettingsView.swift"
      to: "Sources/MeetingsSettingsView.swift"
      via: "MeetingsSettingsView(settings: settings)"
      pattern: "MeetingsSettingsView\\(settings:"
---

<objective>
Extract the Prompts tab (including PromptRowView and NewPromptDialog) into PromptsSettingsView.swift and reduce SettingsView.swift to a thin TabView coordinator.

Purpose: Complete the UI decomposition. After this plan, SettingsView.swift is a ~60-line coordinator that simply instantiates 4 sub-views in a TabView. All tab logic, state, and helpers live in dedicated files.

Output: PromptsSettingsView.swift (with PromptRowView and NewPromptDialog) and a minimal SettingsView.swift.
</objective>

<execution_context>
@/Users/oleksandrpalan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oleksandrpalan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ui-decomposition/01-CONTEXT.md
@.planning/phases/01-ui-decomposition/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From SettingsStore.swift (DO NOT MODIFY) -->
```swift
struct Prompt: Codable, Identifiable {
    let id: String
    var label: String
    var content: String
}

class SettingsStore: ObservableObject {
    // Relevant for Prompts:
    @Published var prompts: [Prompt]
    @Published var selectedPromptId: String?
    func createPrompt(label: String, content: String) -> Prompt
    func updatePrompt(id: String, label: String, content: String)
    func deletePrompt(id: String)
    func selectPrompt(id: String)
}
```

<!-- PromptRowView and NewPromptDialog are already separate structs in SettingsView.swift lines 1093-1251.
     They just need to be moved to PromptsSettingsView.swift. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Prompts tab into PromptsSettingsView.swift</name>
  <read_first>Sources/SettingsView.swift</read_first>
  <files>Sources/PromptsSettingsView.swift, Sources/SettingsView.swift</files>
  <action>
1. Create `Sources/PromptsSettingsView.swift` with:

```swift
import SwiftUI

struct PromptsSettingsView: View {
    @ObservedObject var settings: SettingsStore
```

2. Move these @State variables from SettingsView into PromptsSettingsView:
   - `@State private var showingNewPromptDialog = false`
   - `@State private var newPromptLabel = ""`
   - `@State private var newPromptContent = ""`
   - `@State private var editingPromptId: String? = nil`
   - `@State private var editingPromptLabel = ""`
   - `@State private var editingPromptContent = ""`

3. Move the `promptsTab` computed property content into `body` of PromptsSettingsView. This is the ScrollView with the Enhancement Prompts GroupBox containing: header with "New Prompt" button, empty state, selected prompt indicator, LazyVStack with PromptRowView items. Keep `.tabItem { Label("Prompts", systemImage: "text.bubble") }`.

4. Add the `.sheet(isPresented: $showingNewPromptDialog)` modifier to PromptsSettingsView body (move from SettingsView.body). It presents `NewPromptDialog(label: $newPromptLabel, content: $newPromptContent, onCreate: { createNewPrompt() }, onCancel: { cancelNewPrompt() })`.

5. Move these prompt management functions from SettingsView into PromptsSettingsView:
   - `private func createNewPrompt()` — validates label, calls settings.createPrompt, resets form, closes dialog
   - `private func cancelNewPrompt()` — resets form, closes dialog
   - `private func startEditing(_ prompt: Prompt)` — sets editing state
   - `private func savePromptEdits(_ promptId: String)` — calls settings.updatePrompt, cancels editing
   - `private func cancelEditing()` — clears editing state

6. Move `PromptRowView` struct (lines 1095-1191 of original) and `NewPromptDialog` struct (lines 1195-1251 of original) into `PromptsSettingsView.swift` — place them BELOW the PromptsSettingsView struct in the same file.

7. In `Sources/SettingsView.swift`:
   - Remove `promptsTab` computed property
   - Remove @State variables listed in step 2
   - Remove `.sheet(isPresented: $showingNewPromptDialog)` from body
   - Remove prompt management functions (createNewPrompt, cancelNewPrompt, startEditing, savePromptEdits, cancelEditing)
   - Remove `PromptRowView` struct
   - Remove `NewPromptDialog` struct
   - Replace `promptsTab` reference in TabView with `PromptsSettingsView(settings: settings)`

8. Run `swift build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Sources/PromptsSettingsView.swift` exists
    - `grep -c "struct PromptsSettingsView: View" Sources/PromptsSettingsView.swift` returns 1
    - `grep -c "@ObservedObject var settings: SettingsStore" Sources/PromptsSettingsView.swift` returns 1
    - `grep -c "struct PromptRowView: View" Sources/PromptsSettingsView.swift` returns 1
    - `grep -c "struct NewPromptDialog: View" Sources/PromptsSettingsView.swift` returns 1
    - `grep -c "createNewPrompt" Sources/PromptsSettingsView.swift` is greater than 0
    - `grep -c "startEditing" Sources/PromptsSettingsView.swift` is greater than 0
    - `grep -c "showingNewPromptDialog" Sources/PromptsSettingsView.swift` is greater than 0
    - `grep -c "promptsTab" Sources/SettingsView.swift` returns 0
    - `grep -c "PromptRowView" Sources/SettingsView.swift` returns 0
    - `grep -c "NewPromptDialog" Sources/SettingsView.swift` returns 0
    - `grep -c "createNewPrompt" Sources/SettingsView.swift` returns 0
    - `grep -c "PromptsSettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `swift build` succeeds
  </acceptance_criteria>
  <done>PromptsSettingsView.swift contains the full Prompts tab, PromptRowView, and NewPromptDialog. SettingsView.swift references PromptsSettingsView(settings: settings). Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and clean SettingsView.swift as thin TabView coordinator</name>
  <read_first>Sources/SettingsView.swift</read_first>
  <files>Sources/SettingsView.swift</files>
  <action>
1. Read `Sources/SettingsView.swift` and verify it is now a thin TabView coordinator. The final state should look approximately like this:

```swift
import SwiftUI

struct SettingsView: View {
    @StateObject private var settings = SettingsStore.shared

    var body: some View {
        TabView {
            GeneralSettingsView(settings: settings)
            HotkeySettingsView(settings: settings)
            MeetingsSettingsView(settings: settings)
            PromptsSettingsView(settings: settings)
        }
        .frame(minWidth: 500, minHeight: 400)
        .background(Color.black)
        .preferredColorScheme(.dark)
    }
}
```

2. Remove any leftover code that should not be in the coordinator:
   - Any remaining @State variables (there should be NONE except possibly UI-only ones like frame size which don't exist)
   - Any remaining private computed properties (generalTab, hotKeyTab, etc.)
   - Any remaining private functions
   - Any remaining .onChange handlers (all should be in sub-views now)
   - Any remaining .onAppear handlers (moved to sub-views)
   - Any remaining .alert or .sheet modifiers (moved to sub-views)

3. Ensure only `@StateObject private var settings = SettingsStore.shared` remains as state — this is the single source of truth passed to all sub-views.

4. Count the lines: `wc -l Sources/SettingsView.swift`. Target: 80 lines or fewer. If it's over 80 lines, look for unnecessary blank lines or leftover MARK comments to remove.

5. Run `swift build` to verify final compilation.
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5 && echo "---" && wc -l Sources/SettingsView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l Sources/SettingsView.swift` shows 80 or fewer lines
    - `grep -c "@StateObject private var settings = SettingsStore.shared" Sources/SettingsView.swift` returns 1
    - `grep -c "GeneralSettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `grep -c "HotkeySettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `grep -c "MeetingsSettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `grep -c "PromptsSettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `grep -c "private func" Sources/SettingsView.swift` returns 0
    - `grep -c "private var.*some View" Sources/SettingsView.swift` returns 0
    - `grep -c "@State" Sources/SettingsView.swift` returns 0
    - `swift build` succeeds
  </acceptance_criteria>
  <done>SettingsView.swift is a thin TabView coordinator with 80 or fewer lines, no @State, no private functions, no private computed views. It only contains @StateObject for SettingsStore.shared and a TabView body with 4 sub-view instantiations. Project compiles.</done>
</task>

</tasks>

<verification>
- `swift build` passes
- `Sources/PromptsSettingsView.swift` exists with PromptsSettingsView, PromptRowView, and NewPromptDialog
- `Sources/SettingsView.swift` is 80 lines or fewer
- `Sources/SettingsView.swift` contains no @State, no private functions, no private computed views
- All 4 sub-views are instantiated in TabView: GeneralSettingsView, HotkeySettingsView, MeetingsSettingsView, PromptsSettingsView
- All 6 files exist: SettingsView.swift, SettingsViewData.swift, GeneralSettingsView.swift, HotkeySettingsView.swift, MeetingsSettingsView.swift, PromptsSettingsView.swift
</verification>

<success_criteria>
- PromptsSettingsView.swift contains the complete Prompts tab with PromptRowView and NewPromptDialog
- SettingsView.swift is a thin TabView coordinator (80 lines or fewer, no business logic)
- The full Phase 1 decomposition is complete: 1,251-line monolith split into 6 focused files
- swift build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-decomposition/01-03-SUMMARY.md`
</output>
