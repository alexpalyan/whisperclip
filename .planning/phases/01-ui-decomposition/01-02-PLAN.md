---
phase: 01-ui-decomposition
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Sources/HotkeySettingsView.swift
  - Sources/MeetingsSettingsView.swift
  - Sources/SettingsView.swift
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "HotkeySettingsView.swift renders the Hot Key tab identically to the original"
    - "MeetingsSettingsView.swift renders the Meetings tab identically to the original"
    - "Hotkey changes persist through HotkeyManager (updateHotkey/updateMeetingHotkey work)"
    - "Project compiles after each task with swift build"
  artifacts:
    - path: "Sources/HotkeySettingsView.swift"
      provides: "Hot Key settings tab view"
      contains: "struct HotkeySettingsView"
    - path: "Sources/MeetingsSettingsView.swift"
      provides: "Meetings settings tab view"
      contains: "struct MeetingsSettingsView"
    - path: "Sources/SettingsView.swift"
      provides: "SettingsView with HotKey and Meetings tabs extracted"
  key_links:
    - from: "Sources/HotkeySettingsView.swift"
      to: "Sources/SettingsViewData.swift"
      via: "SettingsViewData.modifierOptions, SettingsViewData.keyOptions, SettingsViewData.meetingKeyOptions"
      pattern: "SettingsViewData\\."
    - from: "Sources/HotkeySettingsView.swift"
      to: "Sources/SettingsStore.swift"
      via: "@ObservedObject var settings: SettingsStore"
      pattern: "@ObservedObject var settings: SettingsStore"
    - from: "Sources/MeetingsSettingsView.swift"
      to: "Sources/SettingsViewData.swift"
      via: "SettingsViewData.summaryLanguageOptions"
      pattern: "SettingsViewData\\."
    - from: "Sources/MeetingsSettingsView.swift"
      to: "Sources/SettingsStore.swift"
      via: "@ObservedObject var settings: SettingsStore"
      pattern: "@ObservedObject var settings: SettingsStore"
    - from: "Sources/SettingsView.swift"
      to: "Sources/HotkeySettingsView.swift"
      via: "HotkeySettingsView(settings: settings) in TabView"
      pattern: "HotkeySettingsView\\(settings:"
    - from: "Sources/SettingsView.swift"
      to: "Sources/MeetingsSettingsView.swift"
      via: "MeetingsSettingsView(settings: settings) in TabView"
      pattern: "MeetingsSettingsView\\(settings:"
---

<objective>
Extract the Hot Key tab and Meetings tab into their own files.

Purpose: Continue incremental decomposition. HotkeySettingsView gets all hotkey-related @State, pickers, and helper functions (updateHotkey, updateMeetingHotkey, keyCodeToString, etc.). MeetingsSettingsView gets all meeting-related UI (auto-detection, auto-start/stop, summary language, monitored apps).

Output: Two new files (HotkeySettingsView.swift, MeetingsSettingsView.swift) and a further-reduced SettingsView.swift.
</objective>

<execution_context>
@/Users/oleksandrpalan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oleksandrpalan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ui-decomposition/01-CONTEXT.md
@.planning/phases/01-ui-decomposition/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From SettingsViewData.swift (created in Plan 01) -->
```swift
enum SettingsViewData {
    static let languageOptions: [(String, String)]
    static let summaryLanguageOptions: [(String, String)]
    static let modifierOptions: [(UInt, String)]
    static let meetingKeyOptions: [(UInt16, String)]
    static let keyOptions: [(UInt16, String)]
}
```

<!-- From SettingsStore.swift (DO NOT MODIFY) -->
```swift
class SettingsStore: ObservableObject {
    static let shared = SettingsStore()
    // Relevant @Published for HotKey tab:
    //   hotkeyEnabled, hotkeyModifier (NSEvent.ModifierFlags), hotkeyKey (UInt16)
    //   meetingHotkeyEnabled, meetingHotkeyModifier, meetingHotkeyKey
    //   holdToTalk
    // Relevant @Published for Meetings tab:
    //   meetingAutoDetect, meetingAutoStart, meetingAutoStop, meetingAutoStopDelay
    //   meetingAutoSummary, meetingSummaryLanguage, meetingDetectedApps
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Hot Key tab into HotkeySettingsView.swift</name>
  <read_first>Sources/SettingsView.swift, Sources/SettingsViewData.swift</read_first>
  <files>Sources/HotkeySettingsView.swift, Sources/SettingsView.swift</files>
  <action>
1. Create `Sources/HotkeySettingsView.swift` with:

```swift
import SwiftUI
import Cocoa

struct HotkeySettingsView: View {
    @ObservedObject var settings: SettingsStore
```

2. Move these @State variables from SettingsView into HotkeySettingsView:
   - `@State private var selectedModifierRawValue: UInt = NSEvent.ModifierFlags.command.rawValue`
   - `@State private var hotkeyKeyString: String = "Space"`
   - `@State private var selectedKeyCode: UInt16 = 49`
   - `@State private var meetingModifierRawValue: UInt = NSEvent.ModifierFlags.control.rawValue`
   - `@State private var meetingKeyString: String = "M"`
   - `@State private var meetingKeyCode: UInt16 = 46`

3. Move the `hotKeyTab` computed property content into `body` of HotkeySettingsView. This is the ScrollView with 3 GroupBoxes: global hotkey toggle+pickers, recording mode (hold-to-talk), meeting hotkey toggle+pickers. Keep `.tabItem { Label("Hot Key", systemImage: "keyboard") }`.

4. Move these helper functions from SettingsView into HotkeySettingsView:
   - `private func loadCurrentSettings()` — BUT only keep the hotkey-related lines:
     ```swift
     private func loadHotkeySettings() {
         selectedModifierRawValue = settings.hotkeyModifier.rawValue
         selectedKeyCode = settings.hotkeyKey
         hotkeyKeyString = keyCodeToString(settings.hotkeyKey)
         meetingModifierRawValue = settings.meetingHotkeyModifier.rawValue
         meetingKeyCode = settings.meetingHotkeyKey
         meetingKeyString = meetingKeyCodeToString(settings.meetingHotkeyKey)
     }
     ```
   - `private func updateHotkey()` (calls HotkeyManager.shared.updateSystemHotkey)
   - `private func updateMeetingHotkey()` (calls HotkeyManager.meetingShared.updateSystemHotkey)
   - `private func getModifierString() -> String` (iterates SettingsViewData.modifierOptions)
   - `private func getMeetingModifierString() -> String` (iterates SettingsViewData.modifierOptions)
   - `private func keyCodeToString(_ keyCode: UInt16) -> String` (iterates SettingsViewData.keyOptions)
   - `private func meetingKeyCodeToString(_ keyCode: UInt16) -> String` (iterates SettingsViewData.meetingKeyOptions)

5. Add `.onAppear { loadHotkeySettings() }` to the HotkeySettingsView body.

6. Move these `.onChange` handlers from SettingsView.body into HotkeySettingsView body:
   - `.onChange(of: settings.hotkeyModifier)` — updates selectedModifierRawValue
   - `.onChange(of: settings.hotkeyKey)` — updates selectedKeyCode and hotkeyKeyString

7. In all moved code, replace bare array references with `SettingsViewData.` prefix:
   - `modifierOptions` -> `SettingsViewData.modifierOptions`
   - `keyOptions` -> `SettingsViewData.keyOptions`
   - `meetingKeyOptions` -> `SettingsViewData.meetingKeyOptions`

8. In `Sources/SettingsView.swift`:
   - Remove `hotKeyTab` computed property
   - Remove @State variables listed in step 2
   - Remove functions: `loadCurrentSettings`, `updateHotkey`, `updateMeetingHotkey`, `getModifierString`, `getMeetingModifierString`, `keyCodeToString`, `meetingKeyCodeToString`
   - Remove `.onChange(of: settings.hotkeyModifier)` and `.onChange(of: settings.hotkeyKey)` from body
   - Remove `.onAppear { loadCurrentSettings() }` from body (now handled by HotkeySettingsView)
   - Replace `hotKeyTab` reference in TabView with `HotkeySettingsView(settings: settings)`

9. Run `swift build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Sources/HotkeySettingsView.swift` exists
    - `grep -c "struct HotkeySettingsView: View" Sources/HotkeySettingsView.swift` returns 1
    - `grep -c "@ObservedObject var settings: SettingsStore" Sources/HotkeySettingsView.swift` returns 1
    - `grep -c "updateHotkey" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "updateMeetingHotkey" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "keyCodeToString" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "getModifierString" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "SettingsViewData\\.modifierOptions" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "SettingsViewData\\.keyOptions" Sources/HotkeySettingsView.swift` is greater than 0
    - `grep -c "hotKeyTab" Sources/SettingsView.swift` returns 0
    - `grep -c "updateHotkey" Sources/SettingsView.swift` returns 0
    - `grep -c "keyCodeToString" Sources/SettingsView.swift` returns 0
    - `grep -c "HotkeySettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `swift build` succeeds
  </acceptance_criteria>
  <done>HotkeySettingsView.swift contains the full Hot Key tab with all hotkey @State variables, picker UI, and helper functions. SettingsView.swift references HotkeySettingsView(settings: settings). Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Meetings tab into MeetingsSettingsView.swift</name>
  <read_first>Sources/SettingsView.swift, Sources/SettingsViewData.swift</read_first>
  <files>Sources/MeetingsSettingsView.swift, Sources/SettingsView.swift</files>
  <action>
1. Create `Sources/MeetingsSettingsView.swift` with:

```swift
import SwiftUI

struct MeetingsSettingsView: View {
    @ObservedObject var settings: SettingsStore
```

2. Move the @State variable from SettingsView (if it still exists there — it was used by the Meetings tab summary language picker):
   - `@State private var selectedSummaryLanguage: String = "auto"`

3. Move the `meetingsTab` computed property content into `body` of MeetingsSettingsView. This is the ScrollView with 4 GroupBoxes: auto-detection toggle, automatic recording (auto-start/stop + delay slider), auto-summary + summary language picker, monitored apps grid. Keep `.tabItem { Label("Meetings", systemImage: "text.bubble.fill") }`.

4. Move these helper views from SettingsView into MeetingsSettingsView:
   - `private var autoStopDelaySection: some View` (the @ViewBuilder section with slider)
   - `private var monitoredAppsSection: some View` (the LazyVGrid with MeetingSource toggles)

5. Add `.onAppear { selectedSummaryLanguage = settings.meetingSummaryLanguage }` to the MeetingsSettingsView body.

6. Move the `.onChange(of: settings.meetingSummaryLanguage)` handler from SettingsView.body into MeetingsSettingsView body if it still exists.

7. In the summary language picker, replace `summaryLanguageOptions` with `SettingsViewData.summaryLanguageOptions`.

8. In `Sources/SettingsView.swift`:
   - Remove `meetingsTab` computed property
   - Remove `autoStopDelaySection` and `monitoredAppsSection` computed properties
   - Remove `@State private var selectedSummaryLanguage` if still present
   - Remove `.onChange(of: settings.meetingSummaryLanguage)` from body if still present
   - Replace `meetingsTab` reference in TabView with `MeetingsSettingsView(settings: settings)`

9. Run `swift build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/oleksandrpalan/Development/alexpalyan/whisperclip && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Sources/MeetingsSettingsView.swift` exists
    - `grep -c "struct MeetingsSettingsView: View" Sources/MeetingsSettingsView.swift` returns 1
    - `grep -c "@ObservedObject var settings: SettingsStore" Sources/MeetingsSettingsView.swift` returns 1
    - `grep -c "autoStopDelaySection" Sources/MeetingsSettingsView.swift` is greater than 0
    - `grep -c "monitoredAppsSection" Sources/MeetingsSettingsView.swift` is greater than 0
    - `grep -c "MeetingSource" Sources/MeetingsSettingsView.swift` is greater than 0
    - `grep -c "SettingsViewData\\.summaryLanguageOptions" Sources/MeetingsSettingsView.swift` is greater than 0
    - `grep -c "meetingsTab" Sources/SettingsView.swift` returns 0
    - `grep -c "autoStopDelaySection" Sources/SettingsView.swift` returns 0
    - `grep -c "monitoredAppsSection" Sources/SettingsView.swift` returns 0
    - `grep -c "MeetingsSettingsView(settings:" Sources/SettingsView.swift` returns 1
    - `swift build` succeeds
  </acceptance_criteria>
  <done>MeetingsSettingsView.swift contains the full Meetings tab with auto-detection, auto-recording, summary language, and monitored apps sections. SettingsView.swift references MeetingsSettingsView(settings: settings). Project compiles.</done>
</task>

</tasks>

<verification>
- `swift build` passes after each task
- `Sources/HotkeySettingsView.swift` exists with `struct HotkeySettingsView: View`
- `Sources/MeetingsSettingsView.swift` exists with `struct MeetingsSettingsView: View`
- `Sources/SettingsView.swift` has no `hotKeyTab`, `meetingsTab`, `autoStopDelaySection`, or `monitoredAppsSection`
- SettingsView TabView uses `HotkeySettingsView(settings: settings)` and `MeetingsSettingsView(settings: settings)`
</verification>

<success_criteria>
- HotkeySettingsView.swift contains all hotkey UI, state, and helper functions
- MeetingsSettingsView.swift contains all meetings UI and helper sections
- SettingsView.swift is further reduced (only promptsTab and minimal state remain)
- swift build succeeds with zero errors after each task
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-decomposition/01-02-SUMMARY.md`
</output>
